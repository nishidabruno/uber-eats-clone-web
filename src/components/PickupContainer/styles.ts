import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;
  display: flex;
  /* max-height: calc(100% - 96px);
  min-height: calc(100% - 96px); */

  border-top: 2px solid var(--shape_dark);
`;
